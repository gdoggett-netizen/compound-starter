{
  "name": "compound-starter",
  "version": "1.0.0",
  "description": "A layered curriculum for building your own AI second brain",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && echo 'Built — open dist/index.html'"
  },
  "dependencies": {
    "marked": "^12.0.0"
  }
}
